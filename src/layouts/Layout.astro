---
import LogoText from "../components/LogoText.astro";
import HorizonGradient from "../components/HorizonGradient.astro";
import Footer from "../components/Footer.astro";
import NavBar from "../components/NavBar.astro";
import RainbowGradient from "../components/RainbowGradient.astro";
import LightCircles from "../components/LightCircles.astro";

interface Props {
  title: string;
}

const { title = "Start" } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>Madeyski.org | {title}</title>
    <meta name="description" content="My personal website" />
	</head>
	<body class="bg-gray-400 overflow-hidden">
    <HorizonGradient />
    <RainbowGradient />
    <LightCircles />
    <LogoText />
    <NavBar />

    <div class="min-h-100 mt-6 px-10 py-8 container w-full max-w-screen-lg mx-auto text-shadow-sm bg-white/60 backdrop-blur-lg rounded-3xl shadow-md/30">
      <slot />
    </div>

    <Footer />
	</body>
</html>

<style>
  @reference "tailwindcss";

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}

  body {
    background-image: conic-gradient(from 180deg,
      var(--color-teal-200),
      var(--color-slate-400),
      var(--color-blue-200),
      var(--color-slate-400),
      var(--color-blue-200),
      var(--color-slate-400),
      var(--color-gray-200),
      var(--color-teal-200)
    );
  }
</style>
